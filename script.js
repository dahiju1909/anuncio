const avanca = const avançar = document.querySelectorAll('.btn-proximo');
const avançar = document.querySelectorAll('.btn-proximo');

avanca.forEach()
const  = document.querySelectorAll('.btn-proximo');

avanca.forEach(button => {
    button.addEventListener()

})
const = documentquerySelectorAll('.btn-proximo');

avanca.forEach(button => {
    button.addEventListener('click', function(){
        const atual - document.querySelector('.ativo');
        const proximoPasso = 'passo-' + this.getAttribute()
     })
})